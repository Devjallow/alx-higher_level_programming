#!/usr/bin/node

const languages = ['C', 'python', 'Javascript'];
const descriptive = ['is fun', 'is cool', 'is amazing'];

for (let i = 0; i < languages.length; i++) {
  console.log(`${languages[i]} ${descriptive[i]}`);
}
